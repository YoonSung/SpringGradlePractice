subprojects {
    apply plugin: "java"
    apply plugin: "war"
    apply plugin: "idea"

    sourceCompatibility = 1.7
    targetCompatibility = 1.7

    repositories {
        mavenCentral()
    }

    dependencies {
        testCompile group: 'junit', name: 'junit', version: '4.11'
    }
}

version = '1.0'


dependencies {
}